import "mocha";
import assert from "assert";

describe('Init command test', () =>
{
    it('create the config file', () => {
        assert.equal(1 + 1, 2);
    });
    
    it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });
});